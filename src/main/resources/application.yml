spring:
  application:
    name: inovapredial
  datasource:
    url: jdbc:postgresql://localhost:5432/dbinovapredial
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
